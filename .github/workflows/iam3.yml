name: IamMaYun

on:
#  push:
#    branches: [ master ]
  schedule:
    - cron: '30 0-2 * * *'

env:
  DING_TOKEN: ${{ secrets.DING_TOKEN }}
  REDIS_PASSWORD: ${{ secrets.REDIS_PASSWORD }}
  PARENT_USER: atmayun

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-python@v2
      with:
        python-version: '3.6'

    - name: requirements
      run: |
        pip3 install -r requirements.txt

    - name: Run
      run: |
        python3 main.py --client huawei_iam --username ${{ secrets.MAYUN_USERNAME }} --password ${{ secrets.PASSWORD }} --git ${{ secrets.MAYUN_GIT }}